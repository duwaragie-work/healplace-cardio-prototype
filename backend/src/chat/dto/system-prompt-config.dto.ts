// Kept as an interface for backwards compatibility with saveConversation signature.
// Legacy menopause fields (medicalLens, tone, etc.) have been removed.
// eslint-disable-next-line @typescript-eslint/no-empty-interface
export interface SystemPromptConfig {}
