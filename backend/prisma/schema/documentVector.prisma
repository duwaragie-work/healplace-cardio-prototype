model DocumentVector {
  id                 String                 @id @default(uuid())
  content            String
  embedding          Unsupported("vector")?
  documentId         String
  sourceActiveStatus Boolean                @default(true)
  document           Document               @relation(fields: [documentId], references: [id], onDelete: Cascade)
}
